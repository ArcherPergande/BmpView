# TODO

0. Bug testing/fixing.

1. Switch to asynchronous reading and writing operations using the EFI_FILE_PROTOCOL.

2. Implement the ability to change which EFI_GRAPHICS_OUTPUT_PROTOCOL currently displays the image (through key binds).

3. Add the “-h” parameter to display a help menu.

4. Optimize the image navigation.

5. Create some form of benchmark.

6. When a file path is not specified to BmpView instead of failing, load an HII file explorer.
